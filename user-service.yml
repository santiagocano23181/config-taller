spring:
  application:
    name: user-service
  profiles:
    active: dev
server:
  servlet:
    context-path: /cinema/api/v1
  port: 9060
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9010/eureka